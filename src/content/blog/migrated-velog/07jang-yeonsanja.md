---
title: "07장. 연산자"
description: "연산자는 하나 이상의 표현식을 대상으로산술할당비교논리지수연산을 수행해 하나의 값을 만든다.2개의 피연산자를 산술 연산하여 숫자 값을 만든다.'+''-''\\*'/%1개의 피연산자를 산술 연산하여 숫자 값을 만든다.\\++—단항 산술 연산자는 이항 산술 연산자와는 달리 증가"
pubDate: 2024-01-09T07:13:20.108Z
updatedDate: 2026-04-12T13:21:10.293Z
tags: []
category: "general"
slug: "07jang-yeonsanja"
draft: false
originalUrl: "https://velog.io/@siltarre/07장.-연산자"
series: "javascript-deep-dive"
---

# 연산자

연산자는 하나 이상의 표현식을 대상으로

- 산술
- 할당
- 비교
- 논리
- 지수연산

을 수행해 하나의 값을 만든다.

## 산술 연산자

### 이항 산술 연산자

2개의 피연산자를 산술 연산하여 숫자 값을 만든다.

- '+'
- '-'
- '*'
- /
- %

### 단항 산술 연산자

1개의 피연산자를 산술 연산하여 숫자 값을 만든다.

- ++
- —

단항 산술 연산자는 이항 산술 연산자와는 달리 증가/감소(++/—) 연산자는 피연산자의 값을 변경하는 부수 효과가 있다.

증가/감소 연산자는 위치에 의미가 있다.

- 피연산자 앞에 위치하면 먼저 피연산자의 값을 증가/감소 시킨 후 다른 연산을 수행한다.
- 피연산자 뒤에 위치하면 다른 연산을 수행한 후 피연산자의 값을 증가/감소 시킨다

### 문자열 연결 연산자

+연산자는 피연산자 중 하나 이상이 문자열인 경우 문자열 연결 연산자로 동작한다.

## 할당 연산자

할당문은 값으로 평가되는 표현식인 문으로서 할당된 값으로 평가된다.

- =
- +=
- -=
- *=
- /=
- %=

## 비교 연산자

좌항 우항의 피연산자를 비교한 다음 그 결과를 불리언 값으로 반환한다. if나 for문과 같은 제어문의 조건식에서 주로 사용된다.

### 동등/일치 비교 연산자

- ==(동등 비교) : 값 같음
- ===(일치 비교) : 값과 타입 같음
- !=(부동등 비교) : 값 다름
- !==(불일치 비교) : 값과 타입 다름

### 대소 관계 비교 연산자

- '>'
- <
- '>='
- <=

## 삼항 조건 연산자

```jsx
var result = score >= 60 ? 'pass' : 'fail';

//score의 값이 60보다 같거나 크면 true = 'pass' 출력
//score의 값이 60보다 작으면 false = 'fail' 출력
```

## 논리 연산자

- ||(논리합 or)
- &&(논리곱 and)
- !(부정)

## 쉼표 연산자

마지막 피연산자의 평가가 끝나면 마지막 피연산자의 평가 결과를 반환

```jsx
var x, y, z;
x=1, y=2, z=3; // 3
```

## 그룹 연산자

괄호 그룹 연산자를 가장 먼저 평가한다.

```jsx
10 * (2+3); // 50
```

## typeof 연산자

피연산자의 데이터 타입을 문자열로 반환한다.

## 지수 연산자

좌항의 피연산자를 밑으로 우항의 피연산자를 지수로 거듭 제곱하여 숫자값을 반환한다.

## 연산자의 부수 효과

대부분의 연산자는 다른 코드에 영향을 주지않는다.

부수 효과가 있는 연산자는 할당연산자, 증가/감소 연산자,delete 연산자 이다.
